# APIレスポンスのローカルキャッシュ(再実行すれば再生成される)
.cache/

# collect_grap.py の生成物(レビュー用の中間ファイル。再実行すれば再生成される)
scripts/candidates.json
scripts/unclassified.json
scripts/submission_candidates.json

# recover_rejected.py の生成物(再実行すれば再生成される)
scripts/recovered_candidates.json
scripts/recover_rejected.log

# 手動アップロード用の生成物(GitHub連携後は不要)
dist/

.DS_Store

# APIキー(YouTube Data API v3 等)
.env
